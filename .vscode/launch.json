{
  // IntelliSense を使用して利用可能な属性を学べます。
  // 既存の属性の説明をホバーして表示します。
  // 詳細情報は次を確認してください: https://go.microsoft.com/fwlink/?linkid=830387
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Launch: Client",
      "program": "${workspaceFolder}/packages/client/src/client.ts",

      "preLaunchTask": "lerna: build client",
      "outFiles": ["${workspaceFolder}/packages/client/**/*.js"]
    },
    {
      "type": "node",
      "request": "launch",
      "name": "Launch: Server",
      "program": "${workspaceFolder}/packages/server/src/server.ts",

      "preLaunchTask": "lerna: build server",
      "outFiles": ["${workspaceFolder}/packages/server/build/*.js"]
    }
  ]
}
